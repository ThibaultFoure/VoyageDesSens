<nav class="navbar navbar-expand-lg bg-secondary navbar-light sticky-top p-0 d-flex flex-column">
	<div class="topHeader bg-primary d-flex justify-content-center align-items-center p-3 w-100">
		<a class="mx-3" href="{{ path('home') }}">
			<img class="logoText" src="{{ asset('build/images/VDS_Horizontal_Complet_Marigold.png') }}" alt="Logo et texte header"/>
		</a>
		<div class="navbarText d-none d-lg-flex flex-column">
			<span class="text-light fs-3 mx-3">Jennifer Quenault</span>
			<span class="text-light fs-5 mx-3">
				<i>Réflexologue</i>
			</span>
		</div>
	</div>
	<div class="container-fluid">
		<button class="navbar-toggler m-2" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-nav navbar-collapse justify-content-lg-center" id="navbarSupportedContent">
			<a class="nav-item nav-link fs-5 ms-lg-1 me-2 {{ app.request.get('_route') is same as 'home' ? 'active' : '' }}" href="{{ path('home') }}">Accueil</a>
			<a class="nav-item nav-link fs-5 ms-lg-1 me-2 {{ app.request.get('_route') is same as 'presentation' ? 'active' : '' }}" href="{{ path('presentation') }}">Présentation</a>
			<a class="nav-item nav-link fs-5 ms-lg-1 me-2 {{ app.request.get('_route') is same as 'session' ? 'active' : '' }}" href="{{ path('session') }}">Prestations</a>
			<a class="nav-item nav-link fs-5 ms-lg-1 me-2 {{ app.request.get('_route') is same as 'price' ? 'active' : '' }}" href="{{ path('price') }}">Tarifs</a>
			<a class="nav-item nav-link fs-5 ms-lg-1 me-2 {{ app.request.get('_route') is same as 'contact' ? 'active' : '' }}" href="{{ path('contact') }}">Contact</a>
		</div>
	</div>
</nav>
