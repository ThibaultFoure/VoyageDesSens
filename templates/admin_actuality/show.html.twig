{% extends 'base.html.twig' %}

{% block title %}Actuality{% endblock %}

{% block body %}
    <h1>Actuality</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ actuality.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ actuality.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ actuality.content }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ actuality.createdAt ? actuality.createdAt|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ actuality.picture }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ actuality.updatedAt ? actuality.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_actuality_index') }}">back to list</a>

    <a href="{{ path('admin_actuality_edit', {'id': actuality.id}) }}">edit</a>

    {{ include('admin_actuality/_delete_form.html.twig') }}
{% endblock %}
